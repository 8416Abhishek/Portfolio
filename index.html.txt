<!doctype html> <html lang="en"> <head> <meta charset="utf-8" /> <meta name="viewport" content="width=device-width, initial-scale=1" /> <title>Portfolio — Abhishek</title> <style> * { margin: 0; padding: 0; box-sizing: border-box; } body { font-family: Arial, sans-serif; background: radial-gradient(circle at top, #0d0d0d, #000000); color: #fff; overflow-x: hidden; } header { text-align: center; padding: 50px 20px; animation: fadeIn 1.5s ease; } h1 { font-size: 3rem; text-shadow: 0 0 15px #00eaff; animation: glowPulse 3s infinite ease-in-out; } h2 { font-size: 1.5rem; margin-top: 10px; opacity: 0.8; } @keyframes glowPulse { 0% { text-shadow: 0 0 5px #00eaff; } 50% { text-shadow: 0 0 25px #00eaff; } 100% { text-shadow: 0 0 5px #00eaff; } } /* Game Box Styling */ #game-box { width: 400px; height: 350px; margin: 40px auto; border-radius: 20px; position: relative; padding: 10px; background: rgba(0, 0, 0, 0.4); box-shadow: 0 0 25px #0088ffaa, inset 0 0 25px #00eaffaa; animation: neonPulse 3s infinite ease-in-out; } @keyframes neonPulse { 0% { box-shadow: 0 0 10px #00eaff; } 50% { box-shadow: 0 0 30px #00eaff; } 100% { box-shadow: 0 0 10px #00eaff; } } #gameCanvas { width: 100%; height: 100%; border-radius: 20px; display: block; } #score { position: absolute; top: 10px; left: 15px; font-size: 18px; color: #00eaff; font-weight: bold; text-shadow: 0 0 10px #00eaff; } </style> </head> <body> <div class="container"> <header> <div class="avatar">A</div> <div> <h1>Abhishek <span style="font-size:14px;color:var(--muted);font-weight:600">— Data Analyst</span></h1> <p class="lead">Transforming data into insights and building interactive visual experiences.</p> </div> </header> <div class="grid"> <main> <div class="card"> <h2 style="margin:0 0 8px">Mini Game — Pac‑Man Style</h2> <p style="margin:0 0 12px;color:var(--muted)">Use arrow keys to move. Eat dots. Avoid nothing (simple demo).</p> <canvas id="game"></canvas> </div> <section style="margin-top:18px" class="card"> <h3 style="margin-top:0">Projects</h3> <div class="projects"> <div class="project"><strong>Project One</strong><p style="margin:6px 0 0;color:var(--muted)">Data visualisation dashboards</p></div> <div class="project"><strong>Project Two</strong><p style="margin:6px 0 0;color:var(--muted)">ETL automation</p></div> <div class="project"><strong>Project Three</strong><p style="margin:6px 0 0;color:var(--muted)">Machine learning models</p></div> <div class="project"><strong>Project Four</strong><p style="margin:6px 0 0;color:var(--muted)">Performance‑tuned SQL queries</p></div> </div> </section> </main> <aside> <div class="card"> <h3 style="margin-top:0">About Me</h3> <p style="color:var(--muted)">Data Analyst passionate about analytics, dashboards, and insights.</p> <h4 style="margin-top:12px">Skills</h4> <div class="skills"> <span class="skill">Oracle ODI</span> <span class="skill">FDI</span> <span class="skill">MySQL</span> </div> </div> <div class="card" style="margin-top:16px"> <h4 style="margin-top:0">Contact</h4> <p style="color:var(--muted)">your.email@example.com</p> <a href="#" style="color:var(--accent);text-decoration:none">Download resume</a> </div> </aside> </div> <footer>© <span id="year"></span> Abhishek — Interactive Portfolio</footer> </div> <script> document.getElementById('year').textContent = new Date().getFullYear(); // Canvas setup const canvas = document.getElementById('gameCanvas'); const ctx = canvas.getContext('2d'); function resizeGame() { const dpr = window.devicePixelRatio || 1; canvas.width = canvas.clientWidth * dpr; canvas.height = canvas.clientHeight * dpr; ctx.setTransform(dpr, 0, 0, dpr, 0, 0); } resizeGame(); window.addEventListener('resize', () => { resizeGame(); regenerateDots(); createGhosts(ghosts.length); placePacStart(); }); // Game box const padding = 18; function box() { return { x: padding, y: padding, w: canvas.clientWidth - padding * 2, h: canvas.clientHeight - padding * 2 }; } // Score let score = 0; const scoreEl = document.getElementById('score'); function updateScore() { scoreEl.textContent = 'SCORE: ' + score; } updateScore(); // Pac const pac = { x: 60, y: 60, r: 16, speed: 2.6, dx: 0, dy: 0, mouth: 0, mouthDir: 1 }; function placePacStart(){ const b = box(); pac.x = b.x + b.w/4; pac.y = b.y + b.h/2; pac.dx = 0; pac.dy = 0; } placePacStart(); // Dots let dots = []; function spawnDot(x, y){ dots.push({ x, y, r: 4, eaten: false, created: performance.now() }); } function regenerateDots(){ dots = []; const b = box(); const cols = Math.max(5, Math.floor(b.w / 28)); for(let i=0;i<cols;i++){ const x = b.x + 20 + i*28; const jitter = (Math.sin(i*1.3) * 6); const y = b.y + b.h/2 + jitter; spawnDot(x,y); } } regenerateDots(); // Periodic random dots let dotSpawnInterval = setInterval(()=>{ const b = box(); const x = Math.random()*b.w + b.x; const y = Math.random()*b.h + b.y; spawnDot(x,y); }, 900); // Ghosts const ghostColors = ['#ff6384','#ff9f40','#36a2eb','#c9cbff']; const ghosts = []; function createGhosts(n=3){ ghosts.length = 0; const b = box(); for(let i=0;i<n;i++){ ghosts.push({ x: b.x + Math.random()*b.w, y: b.y + Math.random()*b.h, r: 14, speed: 0.9 + Math.random()*1.2, vx: (Math.random()-0.5)*1.2, vy: (Math.random()-0.5)*1.2, color: ghostColors[i%ghostColors.length] }); } } createGhosts(3); // Drawing helpers function roundRect(ctx,x,y,w,h,r,fill,stroke){ if(typeof r==='number') r={tl:r,tr:r,br:r,bl:r}; ctx.beginPath(); ctx.moveTo(x+r.tl,y); ctx.lineTo(x+w-r.tr,y); ctx.quadraticCurveTo(x+w,y,x+w,y+r.tr); ctx.lineTo(x+w,y+h-r.br); ctx.quadraticCurveTo(x+w,y+h,x+w-r.br,y+h); ctx.lineTo(x+r.bl,y+h); ctx.quadraticCurveTo(x,y+h,x,y+h-r.bl); ctx.lineTo(x,y+r.tl); ctx.quadraticCurveTo(x,y,x+r.tl,y); ctx.closePath(); if(fill) ctx.fill(); if(stroke) ctx.stroke(); } function drawNeonBox(){ const b = box(); ctx.save(); ctx.fillStyle = 'rgba(0,0,0,0.35)'; ctx.fillRect(b.x-8,b.y-8,b.w+16,b.h+16); ctx.shadowBlur = 20; ctx.shadowColor = 'rgba(0,234,255,0.9)'; ctx.lineWidth = 3; ctx.strokeStyle = 'rgba(0,234,255,0.95)'; roundRect(ctx,b.x,b.y,b.w,b.h,14,false,true); ctx.restore(); // halo ctx.save(); const grad = ctx.createRadialGradient(b.x+b.w/2,b.y+b.h/2,10,b.x+b.w/2,b.y+b.h/2,Math.max(b.w,b.h)); grad.addColorStop(0,'rgba(0,234,255,0.06)'); grad.addColorStop(1,'rgba(0,0,0,0)'); ctx.fillStyle = grad; ctx.fillRect(b.x-30,b.y-30,b.w+60,b.h+60); ctx.restore(); } function drawDots(){ dots.forEach(d=>{ if(d.eaten) return; ctx.beginPath(); ctx.fillStyle='#fff'; ctx.arc(d.x,d.y,d.r,0,Math.PI*2); ctx.fill(); }); } function drawPac(){ ctx.save(); ctx.translate(pac.x,pac.y); const ang = Math.atan2(pac.dy,pac.dx); let rot = 0; if(!(pac.dx===0 && pac.dy===0)) rot = ang; ctx.rotate(rot); pac.mouth += 0.2*pac.mouthDir; if(pac.mouth>0.8||pac.mouth<0) pac.mouthDir*=-1; const open = 0.2 + Math.abs(Math.sin(pac.mouth))*0.6; ctx.beginPath(); ctx.fillStyle='#ffd400'; ctx.moveTo(0,0); ctx.arc(0,0,pac.r,open*Math.PI,(2-open)*Math.PI); ctx.lineTo(0,0); ctx.fill(); ctx.restore(); } function drawGhost(g){ ctx.save(); ctx.beginPath(); ctx.fillStyle = g.color; ctx.arc(g.x, g.y - g.r*0.2, g.r, Math.PI, 0); ctx.fillRect(g.x - g.r, g.y - g.r*0.2, g.r*2, g.r*1.1); // scallops const scallop = 5; const scallopW = (g.r*2)/scallop; ctx.beginPath(); for(let i=0;i<scallop;i++){ const sx = g.x - g.r + i*scallopW + scallopW/2; ctx.arc(sx, g.y + g.r*0.3, scallopW/2, 0, Math.PI); } ctx.fill(); // eyes ctx.fillStyle='#fff'; ctx.beginPath(); ctx.arc(g.x - g.r*0.4, g.y - g.r*0.2, g.r*0.28, 0, Math.PI*2); ctx.arc(g.x + g.r*0.2, g.y - g.r*0.2, g.r*0.28, 0, Math.PI*2); ctx.fill(); ctx.fillStyle='#000'; ctx.beginPath(); ctx.arc(g.x - g.r*0.35, g.y - g.r*0.18, g.r*0.12, 0, Math.PI*2); ctx.arc(g.x + g.r*0.25, g.y - g.r*0.18, g.r*0.12, 0, Math.PI*2); ctx.fill(); ctx.restore(); } // Update loop function update(dt){ // pac movement pac.x += pac.dx * pac.speed; pac.y += pac.dy * pac.speed; const b = box(); if(pac.x - pac.r < b.x) pac.x = b.x + pac.r; if(pac.x + pac.r > b.x + b.w) pac.x = b.x + b.w - pac.r; if(pac.y - pac.r < b.y) pac.y = b.y + pac.r; if(pac.y + pac.r > b.y + b.h) pac.y = b.y + b.h - pac.r; // dots collision dots.forEach(d=>{ if(d.eaten) return; const dist = Math.hypot(pac.x - d.x, pac.y - d.y); if(dist < pac.r + d.r){ d.eaten = true; score += 10; updateScore(); } }); // ghosts movement ghosts.forEach(g=>{ g.x += g.vx * g.speed; g.y += g.vy * g.speed; if(g.x - g.r < b.x) { g.x = b.x + g.r; g.vx *= -1; } if(g.x + g.r > b.x + b.w) { g.x = b.x + b.w - g.r; g.vx *= -1; } if(g.y - g.r < b.y) { g.y = b.y + g.r; g.vy *= -1; } if(g.y + g.r > b.y + b.h) { g.y = b.y + b.h - g.r; g.vy *= -1; } if(Math.random() < 0.01){ g.vx += (Math.random()-0.5)*0.6; g.vy += (Math.random()-0.5)*0.6; } const maxV = 1.2; g.vx = Math.max(-maxV, Math.min(maxV, g.vx)); g.vy = Math.max(-maxV, Math.min(maxV, g.vy)); // collision with pac const dist = Math.hypot(pac.x - g.x, pac.y - g.y); if(dist < pac.r + g.r - 4){ score = Math.max(0, score - 50); updateScore(); placePacStart(); } }); // remove eaten dots after short delay const now = performance.now(); dots = dots.filter(d => !(d.eaten && now - d.created > 800)); // regenerate if none if(dots.filter(d=>!d.eaten).length === 0){ regenerateDots(); } } // Draw loop function draw(){ ctx.clearRect(0,0,canvas.clientWidth,canvas.clientHeight); drawNeonBox(); drawDots(); ghosts.forEach(drawGhost); drawPac(); } // Game loop let last = performance.now(); function loop(now){ const dt = (now - last) / 16.6667; last = now; update(dt); draw(); requestAnimationFrame(loop); } requestAnimationFrame(loop); // Controls document.addEventListener('keydown', e=>{ if(e.key==='ArrowUp'){ pac.dx=0; pac.dy=-1; } if(e.key==='ArrowDown'){ pac.dx=0; pac.dy=1; } if(e.key==='ArrowLeft'){ pac.dx=-1; pac.dy=0; } if(e.key==='ArrowRight'){ pac.dx=1; pac.dy=0; } }); // Touch let touchStart = null; canvas.addEventListener('touchstart', e=>{ touchStart = e.touches[0]; }); canvas.addEventListener('touchend', e=>{ if(!touchStart) return; const end = e.changedTouches[0]; const dx = end.clientX - touchStart.clientX; const dy = end.clientY - touchStart.clientY; if(Math.abs(dx) > Math.abs(dy)){ pac.dx = dx > 0 ? 1 : -1; pac.dy = 0; } else { pac.dy = dy > 0 ? 1 : -1; pac.dx = 0; } touchStart = null; }); </script> </body> </html>